# 출시 전 재검토 — 2026-09-07

기준: d796d44 및 현재 미커밋 SEO 변경. 코드·자동 테스트·기존 Vercel 실패 로그를 검토했다. 코드 수정, 배포, Paddle 설정 변경은 수행하지 않았다.

## 우선 수정 사항

1. **높음 — 종료 저장 실패가 성공으로 처리될 수 있음.** `app/api/lecture-sessions/route.ts:635`는 마지막 세그먼트 upsert 실패를 로그만 남기고 completed로 진행한다. 클라이언트는 HTTP 성공 시 로컬 복구본을 삭제한다. completed 재요청은 619행에서 조기 반환하므로 유실된 꼬리를 복구하지 못한다. 저장 오류 시 실패 응답을 반환하고 완료 전환을 중단해야 한다.
2. **높음 — 미저장 수업과 현재 수업의 기록 혼합.** `app/classroom/use-lecture-recorder.ts:270`은 pending 수업 ID에 현재 `segmentsRef.current`를 보낸다. 종료 저장 실패 후 다른 수업을 열거나 시작하면 다른 수업의 내용이 전송될 수 있다. pending 항목에 세그먼트 스냅샷을 함께 보관하고 수업별로 재시도해야 한다. 225행의 무조건 pending 초기화 역시 다른 복구 요청과 경합한다. 마운트 시 로컬 복구가 한 번 실패하면 정기 재시도 목록에 들어가지 않는 문제도 있다.
3. **높음 — Paddle 웹훅 등록 스크립트가 결제 완료 이벤트만 등록.** `scripts/paddle-catalog.mjs:87`은 transaction.completed만 구독한다. 기존 destination도 누락 이벤트를 보완하지 않는다. 서버는 subscription.* 및 adjustment.created/updated를 처리한다. Live 설정이 이 스크립트대로라면 환불 크레딧 회수와 구독 상태 동기화가 누락된다. 실제 Live 설정은 이번 검토에서 조회하지 않았으므로 운영 누락은 조건부 판단이다. 필요한 전체 이벤트 목록을 대조·보완해야 한다. 계정 심사·키 교체 등 사용자 담당 작업과 별개인 구현 검토 사항이다.
4. **높음 — 미커밋 .vercelignore가 필수 코드를 제외.** `.vercelignore:19`의 supabase 패턴은 app/lib/supabase까지 제외한다. 실패한 미리보기 lecue-1uwvjc119의 Vercel 로그에서 해당 모듈 누락으로 34개 빌드 오류 확인. 루트 디렉터리로 패턴 범위를 한정하고 업로드 목록 및 미리보기 빌드를 재검증해야 한다. 현재 운영 배포 자체가 실패한 것은 아니다.
5. **중간 — SEO 변경의 API 언어 회귀.** 미커밋 `proxy.ts`는 x-site-locale을 경로로 덮어쓰며 /api 경로는 항상 ko가 된다. 영어 화면이 보내는 X-Site-Locale: en도 사라져 오류 안내·언어별 응답이 한국어로 돌아갈 수 있다. API에서는 검증된 ko/en 요청값을 존중하고 공개 페이지 메타데이터의 경로 기반 언어와 분리해야 한다.

## 확인한 개선 및 한계

- npm test 156/156 통과, npm run typecheck 통과. 장애 상황을 모두 검증한다는 뜻은 아니다.
- 마이크 트랙 종료 감지·죽은 스트림 재연결 방지, WebM 미지원 사전 안내, 오래된 노트 generating 상태의 실패 처리 및 재시도 UI는 코드에 반영돼 있다. 과거 감사의 해당 항목을 미수정으로 재보고하지 않는다.
- 결제 webhook 서명 검증, 주문/가격/환경/구독 소유 연결, 원자적 apply_billing_event 호출을 확인했다. 실제 결제·환불 및 운영 DB 제약의 통합 검증은 수행하지 않았다.
- 온라인 영상 미리보기는 원본 트랙을 중단하지 않으며 video는 muted로 재생한다. 실제 공유 선택창→영상→STT→공유 중단→재개 검증은 여전히 필요하다.
- 온라인 공유 중단 후 오프라인 시간의 과금 오차는 기존 online-lecture-qa.md에 남아 있다. 종료 시 마지막 발화 기준 상한은 구현돼 있지만 일시정지 후 재개의 과금 정확성까지 보장하지 않는다.
- SEO canonical/hreflang/robots/sitemap 구현은 로컬에 존재하지만 빌드 실패로 운영 적용이 완료되지 않았다.
- 이번 검토는 새 브라우저 시각 검수나 실결제 실행을 포함하지 않는다.

## 다음 순서

종료 저장 무결성 → 수업별 복구 큐 → 웹훅 이벤트 대조 → 배포 제외 규칙·API 언어 회귀 수정 → 미리보기 빌드 → 실제 온라인 강의 인수 검사 → 승인된 범위의 운영 배포.

참고: https://developer.paddle.com/webhooks/about/notification-destinations/ — destination별 구독 이벤트를 지정해야 하며 업데이트 시 기존 항목을 포함한 전체 목록을 전달한다.

## 수정 결과 — 후속 작업

- 마지막 기록 저장 실패 시 503으로 종료하고 completed 전환·과금·색인을 진행하지 않도록 수정.
- 미저장 종료를 수업별 스냅샷 큐로 변경. 다른 수업 기록 혼합 방지, 동시 재시도 차단, 새로고침 후 복구 실패도 정기 재시도. 최초 저장 요청 전에 로컬 복구본 기록.
- Paddle 등록 스크립트에 구독/환불 이벤트 추가. 기존 destination의 사용자 지정 이벤트를 보존하면서 누락 이벤트를 보충. 실제 Paddle API 실행·계정 인증·키 변경은 하지 않음.
- `.vercelignore`의 Supabase 제외를 루트에 한정. dry-run으로 앱 Supabase 코드 포함과 환경변수/DB 자료 제외 확인.
- API의 명시적 ko/en 헤더 보존. 공개 페이지 SEO 언어는 경로 기준 유지.
- 자동 테스트 159개 및 타입 검사 통과. Paddle 모의 생성·업데이트 테스트 2건 통과(외부 호출 없음).
- 로컬 HTTP에서 SEO 공개 페이지·canonical/hreflang·robots/sitemap·로그인 noindex·정적 자산 헤더, API 한영 401 응답 확인.
- 로컬 Turbopack은 내부 포트 제한으로 실패. Vercel 미리보기에서 컴파일·타입 검사·44개 페이지 생성 성공.
- 미리보기: https://lecue-okum7a4ej-dbgudwn43890-dels-projects.vercel.app
- 운영 배포 및 실제 공유/STT·결제 인수 검증은 이번 수정에 포함하지 않음.
