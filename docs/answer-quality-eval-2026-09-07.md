# 학습형 답변 실제 모델 평가

## 현재 상태

**사용자가 승인한 합성 질문 4건을 실제 OpenAI API로 평가했고, 네 응답 모두 정상 완료됐다.** 비교표, 번호 풀이와 수식, 영어 확인 문제와 정답, 짧은 사실 답변의 형식 검사를 모두 통과했다. 답변을 직접 읽어 핵심 내용과 정답의 사전 노출 여부도 확인했다.

- 실행: 2026-09-07 14:30 KST
- 평가용 외부 API 요청: 4회, 추가 호출·재시도 없음
- 외부 검색: 0회
- 입력 23,878토큰, 출력 427토큰
- 서버 키: 프로세스 내부 인증용으로만 읽었으며 출력·파일 저장하지 않음
- 인증 쿠키: 열람하지 않음
- 운영 사용자의 강의·문답 데이터: 사용하지 않음

초기 실행은 명시적 전송 승인이 부족하다는 자동 승인 검토에서 거절됐다. 우회 실행하지 않고 평가 자료·목적·대상을 설명한 뒤, 사용자가 이 네 건의 실행을 승인한 이후 진행했다.

## 실행 방식

`/tmp/lecue-learning-eval.mjs`는 서버 환경 파일의 기존 OpenAI 키를 프로세스 안에서 읽으며 키를 출력하지 않는다. API 인증 쿠키를 읽거나 앱의 인증 절차를 우회하지 않는다.

`app/api/ask/route.ts`의 한·영 기본 지시와 학습형 표현 지시 상수를 직접 읽어 사용한다. 별도 사본 프롬프트로 평가하지 않으며, 상수 경계를 찾을 수 없으면 요청 전에 중단한다. API 라우트 자체를 불러오거나 데이터베이스에 접근하지 않는다.

모델 `gpt-5.6-luna`, 추론 `low`, 출력 최대 800토큰, 스트리밍 방식은 현재 기본 답변 경로와 같다. 네 합성 질문을 각각 한 번만 순서대로 실행했고, 클라이언트 재시도는 0으로 설정했다. API 인증·검색 맥락·자료 업로드·저장 경로를 포함한 전체 앱 통합 시험은 아니다.

사용한 생산 프롬프트 상수의 SHA-256은 `30cfafed5686ae71fbf6ef404e3a9e96cd2ad798449a202a5d0b8631c4fa2d5e`다.

## 실제 결과

| 합성 시나리오 | 첫 텍스트 | 완료 | 출력 토큰 | 결과 |
| --- | ---: | ---: | ---: | --- |
| 한국어 주식·채권 비교 | 3.251초 | 5.519초 | 242 | 핵심 구분, 비교표, 100만 원 예시 |
| 한국어 미분 계산 | 2.972초 | 3.878초 | 115 | 번호 단계, 수식, 정답 12 |
| 영어 이해 확인 요청 | 0.804초 | 1.512초 | 61 | 확인 문제 뒤 정답, 앞부분에 정답 노출 없음 |
| 한국어 제출 형식 질문 | 2.299초 | 2.540초 | 9 | “PDF 형식입니다.”로 끝남 |

첫 텍스트까지 평균 2.332초, 완료까지 평균 3.362초였다. 응답 헤더가 아니라 실제 텍스트 delta와 완료 이벤트를 측정했다. 모두 `response.completed`이며 토큰 제한에 따른 미완료 응답은 없었다.

현재 저장된 평가 하네스의 토큰 단가 추정식을 적용하면 총 **약 $0.003724**다. 이는 실제 청구서 확인 결과가 아니다. API 사용량은 입력 중 캐시 읽기 11,932토큰, 캐시 쓰기 11,674토큰으로 보고됐고, 검색 호출이 0이므로 이번 평가에서 검색 호출에 따른 비용은 발생하지 않았다.

내용 검토에서 주식의 소유권과 채권의 반환 청구권 구분, 미분 결과 `6x` 및 `x=2`에서의 값 `12`가 타당했다. 영어 문제는 훈련 정확도 99%·새 데이터 정확도 60%를 제시하고 과적합 가능성을 물었다. 정답도 이를 확정적인 증거로 단정하지 않고 가능성을 설명했다. 단답형에는 불필요한 학습 요소가 붙지 않았다.

## 확인 기준

| 합성 시나리오 | 확인할 사항 |
| --- | --- |
| 한국어 주식·채권 비교 | 핵심 구분과 짧은 비교표 |
| 한국어 미분 계산 | 정답 12, 번호 단계, 이중 달러 수식 구분자 |
| 영어 이해 확인 요청 | `Check yourself`와 `Answer` 섹션, 정답을 앞에서 공개하지 않는지 |
| 한국어 제출 형식 질문 | PDF라는 짧은 답, 불필요한 표·문제 없음 |

자동 검사는 형식과 핵심 값만 확인하며, 이번 네 답변은 내용을 직접 읽어 추가 확인했다. 별표 노출·수식·접힌 정답의 실제 UI는 렌더러 검증 대상이다.

비민감 합성 질문·강의·실제 모델 답변, 첫 토큰 시간, 완료시간, 사용량, 형식 체크 및 내용 검토가 `/tmp/lecue-learning-eval.json`에 저장돼 있다. 이 결과를 렌더러에 넣으면 실제 생성 문장의 표·수식·확인 문제를 확인할 수 있다.

## 이미 끝난 자동 검사

- API 회귀 테스트 7개 통과: Markdown을 스트림·완료 응답·저장 데이터에서 유지하는 경로와 기존 맥락·catchup 동작 포함.
- 평가 하네스 테스트 3개 통과: 바이트 단위로 분할된 한글 스트림, 완료 프레임 누락·오류, 사용량의 캐시 카운터 누락 처리.
- 임시 평가 스크립트 구문 검사 및 생산 한·영 프롬프트 추출 검사 통과. 추출 검사는 키를 불러오거나 네트워크를 호출하지 않았다.
- 하네스는 응답 헤더가 아니라 실제 첫 텍스트와 마지막 프레임까지 측정하도록 수정했다.

자동 회귀 테스트와 실제 모델 평가는 구분해야 한다. 이번 실제 네 표본에서는 새 학습 형식이 동작했지만, 한 번씩 실행한 작은 표본이다. 일반적인 정확도, 언어·과목 전반의 품질, 운영 지연, 학습 효과 향상을 입증하지 않는다.

## 브라우저 검증 후 보완

- 실제 결과를 실제 `LearningAnswer` 컴포넌트에 주입해 Chrome에서 확인했다. 화면 검사용 임시 route는 배포 전에 제거한다.
- inline 수식이 작은 화면에서 수식 중간에 잘리지 않도록 한 덩어리로 배치하고, 넘치면 해당 수식만 가로 스크롤한다.
- 평가 후 프롬프트에 display 수식의 여는/닫는 `$$`를 독립된 줄에 배치하라는 문장을 한·영으로 추가했다. 이 최종 문구로 추가 API 평가를 실행하지는 않았다. 4건 결과는 위 SHA의 프롬프트 기준이다.
- 합성 응답의 영속 사본: `docs/answer-quality-samples-2026-09-07.json`. 키·운영 데이터 없음.
