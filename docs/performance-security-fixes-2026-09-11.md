# 통합 릴리스 검증 — 2026-09-11

**현재 운영:** [www.lecue.app](https://www.lecue.app), 최종 배포 `dpl_AhZMnnaLsUVRm1xHGL2h8P3Gz52V`. 통합 보안·녹음·UI 배포와 아래 그래프 렌더러 후속 수정까지 반영됐다.

기록 저장·복구, 요약 소유권·비용 제한, PDF 자원 제한, 일시정지·마이크 선택과 최신 강의실 UI 변경을 한 후보에 통합했다. 운영에 반영했으며, 사용자 요청대로 강의실 사이드바는 기존 `Lecue.` 워드마크를 복원하고, 랜딩 심볼·아이콘과 정렬 보정은 유지했다.

## 통합 배포와 적용 상태

| 항목 | 확인 결과 |
| --- | --- |
| 배포 | [dpl_3zyukobqy5nbSkmJC6JqqhcKXg7H](https://lecue-581qqynl2-dbgudwn43890-dels-projects.vercel.app), **READY** |
| 소스 | `/private/tmp/lecue-consolidated-release-zook9gwv`, **390개** |
| 배포 입력 검증 | Vercel에 업로드된 390개 SHA-1이 동결 manifest와 전부 일치 |
| 앱 실행 지역 설정 | `icn1` — 전역 Middleware와 구분 |
| 운영 승격 | 2026-09-11 17:03:47 KST 확인. `www.lecue.app`이 위 배포 ID를 반환함 |
| 실제 온라인 탭 오디오 | 실제 Chrome 탭 공유·합성 음성 전사·1분 경계·일시정지·재개·종료 통과. DB: completed, 162초, 4구간·768자, 합성 문장 일치 확인 |
| 최종 요약 권한 migration | 2026-09-11 17:09:07 KST 적용. 권한 조건 6개 모두 true, 완료 이력 누락 0. 실제 합성 계정으로 직접 INSERT/UPDATE/DELETE 거부·소유자 조회·서버 claim/complete·재사용 차단 확인 |

환경 파일, 개인 interview 자료, 문서·스크립트와 로컬 생성 실행물은 배포 소스에서 제외했다. 녹음 중에는 소스를 바꾸거나 녹음 탭을 새로고침하지 않았으며, 사용자의 종료 확인 뒤 녹음 변경을 적용했다.

## 통합된 변경

- **저장·복구:** 미확인 구간을 최대 250개씩 보내고 서버가 ACK한 ID만 확인 처리한다. 완료 확인 전에는 복구 사본을 유지한다. 5,001번째 이후 구간을 조용히 버리던 문제를 제거했으며 전체 50,000개 상한 초과는 오류로 알린다. 완료된 수업의 누락 구간은 기존 녹음 시간과 과금 이력 안에서만 복구한다. 기존 클라이언트의 제한 내 최종 저장 요청도 처리한다.
- **동시 녹음 보호:** 수업을 열 때 무조건 pause하던 동작을 없앴다. 복구 API는 활성 relay와 미사용 연결 티켓을 확인한다. 계정 동시 녹음 잠금·크레딧·연결 lease 검사는 유지한다. 이전 수업의 늦은 응답이 새 수업의 저장 상태를 바꾸지 않게 했다.
- **완료 응답·목록:** 기록 완료와 영속 색인 큐 등록을 같은 트랜잭션으로 저장하고, 임베딩 완료를 기다리지 않고 응답한다. 기존 색인은 새 결과가 준비된 뒤 원자적으로 교체한다. 저장 응답으로 목록의 해당 수업을 갱신하며 늦은 이름·폴더 변경과 오래된 조회 응답이 최신 상태를 덮어쓰지 않게 했다. 개인정보 없는 `Server-Timing`과 서울 지역 설정을 적용했다.
- **일시정지·마이크 메뉴:** 입력 정지와 마지막 음성 정리 상태를 분리해 즉시 일시정지를 표시한다. WebSocket 종료가 확인된 때만 서버 pause 요청을 기존 1.2초 유예와 겹친다. PCM·마지막 전사 대기와 재개 차단은 유지한다. 409를 저장 성공으로 간주하지 않으며 실패는 재시도한다. 확인 실패 뒤에도 종료·최종 저장은 가능하다. 마이크 메뉴는 기존 전환 함수를 사용하고 한국어·영어, 키보드, 모바일과 전환 중 비활성화를 지원한다.
- **요약·소유권:** 구간별 생성 이력·완료 상태를 유지한다. 구간당 2회, 사용자당 하루 72회·입력 100만 자, 요청당 입력 6만 자·출력 2,000토큰을 제한하고 SDK 자동 재시도를 끈다. 저장과 완료 기록은 원자적으로 처리하며 저장 시 소유권을 재검사한다. 복합 FK로 부모 소유권을 보장한다. 브라우저 직접 쓰기 권한 철회는 위 최종 migration 결과로 별도 확인한다.
- **자료·PDF:** 20MB·500페이지 외에 누적 50만 자·400청크·임베딩 입력 25만 UTF-8 바이트를 제한한다. 파일 일부를 잘라 저장하지 않고 분할을 안내한다. 별도 워커의 30초·V8 힙 제한과 서버 PDF.js의 스트림당 디코딩 32MiB·누적 할당 128MiB 제한을 적용했다. Next/Turbopack이 네이티브 워커 경로를 바꾸던 문제도 수정했다. DB 예약은 수업당 자료 20개, 사용자당 동시 업로드 2개와 하루 100회·입력 200만 자·토큰 상한 합계 100만을 보장하며, 예산 확인 실패 시 유료 색인을 차단한다.
- **휴대폰 중계:** 사용자 HMAC 식별자로 계정당 방 2개, 연결 전 초대 3분, 연결된 방 최대 3시간을 제한한다. 기존 방과 활성 연결을 보존한다. 방 개수 제한은 새 앱 API와 Worker가 함께 적용돼야 한다.
- **강의실 UI·답변 그래프:** 강의실 생성 폼의 겹침을 없애고 이름 라벨·만들기·취소·인라인 오류와 포커스 복귀를 정리했다. 요약창의 ‘최근 내용으로’ 버튼이 스크롤 영역 높이를 바꾸지 않게 하고 크기 변경을 사용자 스크롤로 오인하지 않게 했다. 제한된 `lecue-chart` 데이터로 구성비·비교 막대그래프를 표시한다. 값·크기·개수를 검증하고 HTML·임의 스타일·URL 실행은 허용하지 않는다. 부분 응답의 JSON을 숨기며, 근거 있는 수치에만 그래프를 사용하도록 질문 지침을 추가했다. 준비 카드와 랜딩 로고 간격도 반영했다.

## 검증 결과 — 각 계층을 구분

| 계층 | 결과 |
| --- | --- |
| 최종 동결 소스 | 전체 앱 **785/785**, 타입 검사 통과. 실패·skip 0 |
| 집중 회귀 | recorder·PCM·token·relay·live-assist·phone **209개**, 그래프 관련 **38개 + 경계 조건 13개** 통과. 전체 시험과 범위가 겹치므로 합산하지 않음 |
| 실제 HTTP | 최종 후보·운영·localhost의 승격 전 녹음 사전 검사 **각 7개, 총 21개** 통과 |
| 실제 workerd | 일반 연결·65초 분 경계·버퍼 큐·7초 제어 지연 **4개를 각각 별도 실행해 통과**. 합성 PCM과 모의 공급자 사용 |
| 배포된 휴대폰 Worker | 이전 65초 합성 시험 통과: **1,301프레임·2,081,600바이트**, 재연결·잘못된 권한 거부·방 정리 확인 |
| 실제 로컬 마이크 | 로그인된 Chrome에서 시작·1분 이상 유지·pause·재개·종료 확인. DB에서 **completed, 105초, 전사 1구간** 저장 확인 |
| 사용자 원래 기록 | 사용자 종료 후 DB에서 **completed, 1,449초, 전사 46구간** 보존 확인. 전사 본문 없이 상태·개수만 조회 |
| UI 브라우저 | 강의실 폼의 포커스·간격·넘침, 한국어·영어/밝은·어두운 화면 확인. 요약 추가 중 **1,339프레임** 동안 팝업 위치·높이 유지. 351px 뷰포트에서도 그래프 넘침 없음 |

실제 마이크 시험에서 pause 클릭 뒤 첫 확인은 약 606ms에 `일시정지 / 기록 정리 중…`이었다. 재개 가능까지의 연속 측정은 하지 않았으므로 이를 평균·최대 pause 지연으로 해석하지 않는다. 마이크 시험은 실제 캡처·공급자 전사·최종 저장의 증거이며 전사 정확도 평가는 아니다.

온라인 공유의 최초 자동 조작은 Chrome의 사용자 활성화·포커스 조건에서 거부됐다. 사용자가 테스트 탭 오디오 공유를 직접 허용한 뒤 실제 캡처·전사·저장이 통과했다. 자동 승인 검토가 개인 대화 노출 가능성 때문에 Chrome 전체 접근성 조회를 한 번 차단했으며, 이후에는 특정 Lecue 탭만 읽었다. 개인 대화·화면은 공유하지 않았다.

### 실제 PDF·DB 검증

- 동일 PDF 구현을 사용한 선행 배포 후보의 실제 API에서 정상 2페이지 PDF는 **201**, 두 페이지 색인·원본 저장을 확인했다. 약 65KB에서 64MiB로 팽창하는 합성 PDF는 **422와 분할 안내**로 거부됐고 자료·임베딩 예산을 남기지 않았다. 정상 파일 한 건의 작은 유료 임베딩을 사용했으며 테스트 자료와 합성 계정을 정리했다. 해당 API·워커·준비 스크립트·Next 설정·lockfile 해시는 최종 후보와 일치한다.
- 실제 PDF.js 시험에서 500,001자 추출·비텍스트 압축 팽창·여러 스트림 누적 할당 제한을 확인했다. 정상 24페이지의 처음·중간·마지막 내용 보존과 실제 워커 취소, 모의 타임아웃 정리도 통과했다. 별도 Next 빌드·실행 회귀는 네이티브 워커 경로와 정상/제한 PDF를 검사한다.
- UI 작업 중간의 전체 시험에서는 PDF 워커 시작 오류 1건이 있었으나 해당 코드 변경 없이 단독 시험과 최종 전체 시험에서 통과했다. 최종 785개 통과는 이 이전 실패가 재현되지 않았다는 결과이며, 간헐성의 원인을 확정한 것은 아니다.
- 독립 PostgreSQL에서 기록 복구·활성 relay/티켓·완료 후 유료 구간 복구·5,001개 꼬리 저장·색인 교체를 검사했다. 자료 예약은 기존 19개에서 20개 요청이 경쟁해도 1개만 허용했고 기존 INSERT 경로도 제한했다. 운영과 같은 PostgreSQL 17.6·pgvector 1,536차원 조건의 적용 전 트랜잭션 검증도 통과했다.
- 실제 후보 API에서 미인증 접근, 다른 계정의 부모 연결, 브라우저의 서비스 RPC 호출을 거부하고 최종 배치 ACK·완료 저장·재조회를 확인했다.

## DB 적용과 최종 확인

27개 테이블의 암호화 백업 뒤 추가 migration `20260911010000`, `20260911020000`, `20260911030000`을 적용했다. `20260911040000_summary_server_writes.sql`도 새 운영 API 전환 후 **300초 이상** 기다린 뒤 17:09:07 KST에 트랜잭션으로 적용했다. 브라우저의 직접 요약 쓰기는 테이블·컬럼 권한 모두 철회됐다. 소유자 조회는 RLS로 유지하며 서버 전용 생성·완료 RPC는 정상 동작한다. 합성 계정 2개로 운영 API 검증을 통과했고 계정과 테스트 데이터는 정리했다. 유료 모델·임베딩 호출은 0회였다.

- 검토한 마지막 migration SHA-256: `402f82a8dd8cbcefb0a98b0a2a329d19b90dfb0b2a414643676fa70a9dec0344`
- 적용 후 권한 검사: `/private/tmp/lecue-summary-final-permissions.sql` — 모든 권한 조건 true, 완료 이력 누락 0 확인
- 승격 직후와 최종 DB 적용 후 각각 후보·운영·로컬의 녹음 HTTP 사전 검사 21개를 다시 통과했다. 운영 배포 ID가 검증한390개 소스의 배포와 일치한다.
- 운영 새 문서에서 Lecue 워드마크·강의실 생성 폼을 확인하고, 실제 온라인 테스트 수업을 다시 열어 종료 상태를 확인했다.
- 최종 권한·API 증거: `/private/tmp/lecue-summary-final-permissions.json`, `/private/tmp/lecue-summary-final-api-smoke.json`.

## 남은 한계

- HTTP 사전 검사·모의 공급자·합성 PCM은 실제 음성 시험을 대신하지 않는다. 실제 모델의 그래프 선택·수치 정확도와 전사 정확도는 평가하지 않았다.
- 색인 재시도는 응답 후 작업과 수업 목록 진입 시 복구에 의존한다. 누적 3회 한도에 도달하면 색인이 미완료로 남을 수 있다. 최초 진입의 전체 이력 조회도 남아 있다.
- localStorage가 가득 차거나 차단되면 미저장 구간은 메모리에만 남아 탭 종료 후 복구를 보장할 수 없다.
- PDF 제한은 디코딩 버퍼·V8 힙·실행 시간의 경계이며 모든 네이티브 메모리의 절대 RSS 상한은 아니다. 임베딩 입력의 바이트 제한은 토큰 상한을 보수적으로 계산하므로 다국어 자료가 더 일찍 제한될 수 있다.
- 기존 운영과 조건을 맞춘 전후 지연 비교는 하지 않았다. 소량의 합성 API 요청에서 종료 저장 약 0.26–0.30초를 관측했지만 전체 사용자 지연이나 특정 개선율을 보장하지 않는다.

## 핵심 증거

- 소스·검증: `/private/tmp/lecue-consolidated-release-{shas,sha256,verification}.json`
- 배포·업로드·사전 검사: `/private/tmp/lecue-consolidated-inspect.json`, `/private/tmp/lecue-consolidated-deployed-files.json`, `/private/tmp/lecue-consolidated-preflight-before-promotion.json`
- 전체 시험·타입: `/private/tmp/lecue-consolidated-app-tests.log`, `/private/tmp/lecue-consolidated-typecheck.log`
- 집중 회귀·workerd: `/private/tmp/lecue-pause-focused-final-tests.log`, `/private/tmp/lecue-pause-workerd-{normal,long,queue,control}.log`
- 실제 온라인 오디오: `/private/tmp/lecue-online-recording-evidence.json`
- 실제 기록·PDF·휴대폰: `/private/tmp/lecue-pause-recording-evidence.json`, `/private/tmp/lecue-fix-candidate-api-smoke.json`, `/private/tmp/lecue-fix-phone-live-smoke.json`
- UI 세부 검증: `docs/classroom-ui-fixes-2026-09-11.md`

## 그래프 JSON 노출 후속 수정 — 배포 완료

사용자가 로컬 답변에 채권 그래프 JSON이 노출되는 이미지를 제공했다. 이전 렌더러가 정확한 `lecue-chart` 코드 블록만 처리하고 일반 `json`·언어 없는 블록의 같은 데이터는 코드로 출력하는 경로를 재현했다. 실제 저장 원문 조회는 자동 승인 검토에서 개인정보 접근으로 거부되어 수행하지 않았으므로 원래 답변의 fence 종류나 오래된 클라이언트 여부는 확정하지 않았다.

`answer-markdown.ts`와 해당 회귀 시험 두 파일을 수정했다. 기존 그래프 스키마를 완전히 만족하는 JSON은 언어 없는 블록·JSON 블록에서도 그래프로 표시한다. 일반 JSON·JavaScript는 코드로 남으며, 생성 중인 미완성 JSON 객체는 첫 `{`부터 완성 전까지 보류한다. 새로운 그래프 타입·외부 요청·임의 HTML 실행은 추가하지 않았다. 기존 저장 답변에도 같은 렌더러가 적용된다.

- 전체 앱 **788/788**, 집중 시험50개, 타입 검사 통과. 두 파일의 기계적 UI 검사 지적0개.
- 첨부 이미지의 제목과 두 수치를 사용한 합성 예시로 실제 수정 렌더러를 SSR했다. Chrome의960px·390px iframe 뷰포트에서 일반JSON·정규차트·언어없는블록을 비교해 정상 막대그래프, 값, 줄바꿈을 확인했다. 실제 모델을 호출하거나 원문을 읽은 시험은 아니다.
- 로컬에도 원본 해시 확인 뒤 두 파일만 적용했다. 녹음 탭을 새로고침하거나 서버를 재시작하지 않았다.
- 최종 운영 배포: **dpl_AhZMnnaLsUVRm1xHGL2h8P3Gz52V**, [배포본](https://lecue-cb9js5xdg-dbgudwn43890-dels-projects.vercel.app). 2026-09-11 17:23:22 KST 운영 연결 확인.
- 소스390개가 고정한 manifest와 전부 일치한다. 앞서 배포한 보안·녹음·PDF·UI 변경을 유지하고 두 파일만 교체했다. DB·음성 연결 로직은 추가 변경하지 않았다.
- 후보·운영·로컬 녹음 사전 검사는 승격 전후 각각21개 통과했다. 운영 도메인이 최종 배포 ID를 반환하는 것을 확인했다.
- 증거: `/private/tmp/lecue-chart-fallback-{hashes,source-verification,production-inspect,preflight-after-promotion}.json`, `/private/tmp/lecue-chart-fallback-app-tests.log`.
