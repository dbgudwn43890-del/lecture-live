name: ci

on:
  push:
    branches: [main]
  pull_request:

jobs:
  check:
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 24
          cache: npm
      - name: Restore verified audio decoder
        uses: actions/cache@v4
        with:
          path: .ffmpeg
          key: ${{ runner.os }}-${{ runner.arch }}-ubuntu24-ffmpeg-${{ hashFiles('build-tools/install-audio-ffmpeg.mjs', 'build-tools/download-pinned-source.mjs', 'build-tools/vendor/ffmpeg-*.tar.xz') }}
      - run: npm ci
      - run: npm run typecheck
      - run: npm test
